class Profile {
  var username, password, email, conpassword, docID;
  Map<String, dynamic>? data;

  Profile(
      {this.username,
      this.password,
      this.email,
      this.conpassword,
      this.docID,
      this.data});
}
