class languageChange {
  var input, output, label, inbox, outbox, appbarInput, appberOutput;

  languageChange(
      {this.input,
      this.output,
      this.label,
      this.inbox,
      this.outbox,
      this.appbarInput,
      this.appberOutput});
}
